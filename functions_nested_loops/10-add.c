#include "main.h"

/**
 * add - adds two intergers and returns the result
 * @a: first int to check
 * @b: second int to check
 * Return: the value of two added intergers
 */

int add(int a, int b)
{
	int c = (a + b);

	return (c);
}
